{
  "name": "weekend-sql-to-do-list",
  "version": "1.0.0",
  "description": "[Project Instructions](./INSTRUCTIONS.md), this line may be removed once you have updated the README.md",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server/server.js",
    "start:test": "export NODE_ENV=test && node server/server.js",
    "cypress:run": "cypress run --config-file cypress.config.js",
    "test": "start-server-and-test start:test http://localhost:5001 cypress:run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "cypress": "^13.3.1",
    "nodemon": "^3.0.1",
    "start-server-and-test": "^2.0.1"
  }
}

